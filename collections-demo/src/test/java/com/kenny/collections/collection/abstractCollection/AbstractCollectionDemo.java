package com.kenny.collections.collection.abstractCollection;

/**
 * @author kenny
 * @version 1.0
 * @date 2020/11/26 8:33
 */
public class AbstractCollectionDemo {
    /**
     * protected  AbstractCollection()
     * 唯一的构造函数。
     */

    /**
     * boolean add(E e)
     * 确保此集合包含指定的元素（可选操作）。
     * boolean addAll(Collection<? extends E> c)
     * 将指定集合中的所有元素添加到这个集合（可选操作）。
     * void clear()
     * 从这个集合中移除所有的元素（可选操作）。
     * boolean contains(Object o)
     * 返回 true如果集合包含指定元素。
     * boolean containsAll(Collection<?> c)
     * 返回 true如果这个集合包含指定集合的所有元素。
     * boolean isEmpty()
     * 返回 true如果集合不包含任何元素。
     * abstract Iterator<E> iterator()
     * 返回包含在此集合中的元素的迭代器。
     * boolean remove(Object o)
     * 从这个集合中移除指定元素的一个实例，如果它是存在的（可选操作）。
     * boolean removeAll(Collection<?> c)
     * 删除此集合中包含的所有元素（可选操作）的所有元素（可选操作）。
     * boolean retainAll(Collection<?> c)
     * 仅保留包含在指定集合中的这个集合中的元素（可选操作）。
     * abstract int size()
     * 返回此集合中的元素的数目。
     * Object[] toArray()
     * 返回包含此集合中所有元素的数组。
     * <T> T[] toArray(T[] a)
     * 返回包含此集合中所有元素的数组；返回数组的运行时类型是指定的数组的运行时类型。
     * String toString()
     * 返回此集合的字符串表示形式。
     */
}
